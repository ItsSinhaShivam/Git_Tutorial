# learn from Chai or Code 

## install 

this is some installtion instruction 
or nothing else 

```bash
 npm install
 ```

 Here are some instruction for pushing our File on Github from file Using
Terminal

step 1 --> git config --global uesr.email "email.com"
            git config --global user.name "Your Name"

step 2 -->
        write ===> add ===> Commit 
        (normal file) ===> git add . or file Name ===> git commit -m (some Message)

stpe 3 --> 
        change Branch Master to Main
        git branch -Main 

step 3 --> 
        Make Repository 

step 4 --> 
        git remote add origin "repo-url"
        git remote -v (check fetch or push result) 

step 5 --> 
        git push origin main 
        git push -u origin main 

That's It 
